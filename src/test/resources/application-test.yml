spring:
  datasource:
    url: jdbc:mysql://mysql.danchive-db.svc.cluster.local:3306/danchive?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: danchive
    password: danchive-password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false
    properties:
      hibernate:
        format_sql: true

firebase:
  credentials:
    path: firebase/danchive-firebase-adminsdk-fbsvc-0e59eb133e.json